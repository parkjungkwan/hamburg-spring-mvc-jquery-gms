<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.sts.service.GradeMapper">
<insert id="insertMany" parameterType="list">
	insert into grades
	( subject, score, grade, stu_num, exam_date, pass_exam )
	values 
	 <foreach item="i" collection="list"
      open="(" separator="," close=")">
        ( #{i.subject}, #{i.score}, #{i.grade}, 
        #{i.stuNum}, #{i.examDate}, #{i.passExam} )
  	</foreach>
</insert>
</mapper>